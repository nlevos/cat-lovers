export interface Breed {
  id: string;
  name: string;
  alt_names?: string;
  decription?: string;
  demoImageUrl?: string;

  // // [index: string]: any; // number | string | boolean;

  // filter?: Filter;
  // last_viewed_at?: string;
  // schedule?: Schedule;
  // title?: string;
}
